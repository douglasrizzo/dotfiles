;=====================================================
;
;   To learn more about how to configure Polybar
;   go to https://github.com/jaagr/polybar
;
;   The README contains alot of information
;
;=====================================================

; white and blue
[inactivecolor]
background = #fff
background-alt = #7cf
foreground = #000
foreground-alt = #555
primary = #2bf
secondary = #e05
alert = #c24

; black and orange
[activecolor]
background = #111
background-alt = #444
foreground = #f90
foreground-alt = #555
primary = #fb2
secondary = #e05
alert = #c24

[bar/example]
monitor = ${env:MONITOR:}
width = 100%
height = 26
;offset-x = 1%
;offset-y = 1%
;radius = 6.0
;fixed-center = false
;bottom = true

background = ${activecolor.background}
foreground = ${activecolor.foreground}

line-size = 3
line-color = #f00

border-size = 0
border-color = #00000000

padding-left = 0
padding-right = 0

module-margin-left = 1
module-margin-right = 2

font-0 = Noto Sans:pixelsize=10;0
font-1 = Fira Sans:pixelsize=10;0
font-2 = Font Awesome 5 Free:pixelsize=10;1
font-3 = Font Awesome 5 Free Solid:pixelsize=10;1
font-4 = Font Awesome 5 Brands:pixelsize=10;1

;modules-left = bspwm i3
modules-left = i3
modules-center = xwindow
modules-right = xbacklight filesystem memory cpu wlan eth battery temperature date

tray-position = right
tray-padding = 2
;tray-transparent = true
;tray-background = #0063ff

;wm-restack = bspwm
;wm-restack = i3

;override-redirect = true

;scroll-up = bspwm-desknext
;scroll-down = bspwm-deskprev

;scroll-up = i3wm-wsnext
;scroll-down = i3wm-wsprev

cursor-click = pointer
cursor-scroll = ns-resize

[module/xwindow]
type = internal/xwindow

format = <label>
format-background = ${activecolor.background}
format-foreground = ${activecolor.foreground}
format-padding = 4
;format-underline = ${activecolor.foreground}
;format-overline = ${self.format-underline}

label = %title%
label-maxlen = 50

[module/xkeyboard]
type = internal/xkeyboard
blacklist-0 = num lock

format-prefix = " "
format-prefix-foreground = ${activecolor.foreground-alt}
format-prefix-underline = ${activecolor.secondary}

label-layout = %layout%
label-layout-underline = ${activecolor.secondary}

label-indicator-padding = 2
label-indicator-margin = 1
label-indicator-background = ${activecolor.secondary}
label-indicator-underline = ${activecolor.secondary}

[module/filesystem]
type = internal/fs
interval = 25

mount-0 = /

label-mounted =  %{F#0a81f5}%mountpoint%%{F-}: %percentage_used%%
label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${activecolor.foreground-alt}

;format-underline = #f90000
;format-overline = ${self.format-underline}

[module/bspwm]
type = internal/bspwm

label-focused = %index%
label-focused-background = ${activecolor.background-alt}
label-focused-underline= ${activecolor.primary}
label-focused-padding = 2

label-occupied = %index%
label-occupied-padding = 2

label-urgent = %index%!
label-urgent-background = ${activecolor.alert}
label-urgent-padding = 2

label-empty = %index%
label-empty-foreground = ${activecolor.foreground-alt}
label-empty-padding = 2

[module/i3]
type = internal/i3

; Only show workspaces defined on the same output as the bar
;
; Useful if you want to show monitor specific workspaces
; on different bars
;
; Default: false
pin-workspaces = true

; This will split the workspace name on ':'
; Default: false
strip-wsnumbers = false

; Sort the workspaces by index instead of the default
; sorting that groups the workspaces by output
; Default: false
index-sort = true

; Create click handler used to focus workspace
; Default: true
enable-click = true

; Create scroll handlers used to cycle workspaces
; Default: true
enable-scroll = false

; Wrap around when reaching the first/last workspace
; Default: true
wrapping-scroll = true

; Set the scroll cycle direction 
; Default: true
reverse-scroll = true

; Use fuzzy (partial) matching on labels when assigning 
; icons to workspaces
; Example: code;♚ will apply the icon to all workspaces 
; containing 'code' in the label
; Default: false
;fuzzy-match = true

; Available tags:
;   <label-state> (default) - gets replaced with <label-(focused|unfocused|visible|urgent)>
;   <label-mode> (default)
format = <label-state> <label-mode>

; Available tokens:
;   %mode%
; Default: %mode%
label-mode = %mode%
label-mode-padding = 2
label-mode-foreground = #000
label-mode-background = ${activecolor.primary}

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
;label-focused = %index%
label-focused-foreground = ${activecolor.background}
label-focused-background = ${activecolor.primary}
label-focused-underline = ${activecolor.foreground}
label-focused-padding = 4

;label-unfocused = %index%
label-unfocused-padding = 4

;label-visible = %index%
label-visible-underline = #555555
label-visible-padding = 4

;label-urgent = %index%
label-urgent-foreground = #000000
label-urgent-background = #bd2c40
label-urgent-padding = 4

; Separator in between workspaces
;label-separator = |
<<<<<<< Updated upstream
label-separator-padding = 1
label-separator-foreground = ${activecolor.foreground}
=======
label-separator-padding = 0
label-separator-foreground = #ffb52a
>>>>>>> Stashed changes

[module/mpd]
type = internal/mpd
format-online = <label-song>  <icon-prev> <icon-stop> <toggle> <icon-next>

icon-prev = 
icon-stop = 
icon-play = 
icon-pause = 
icon-next = 

label-song-maxlen = 25
label-song-ellipsis = true

[module/xbacklight]
type = internal/xbacklight

format = <label> <bar>
label = BL

bar-width = 10
bar-indicator = |
bar-indicator-foreground = #ff
bar-indicator-font = 2
bar-fill = ─
bar-fill-font = 2
bar-fill-foreground = #9f78e1
bar-empty = ─
bar-empty-font = 2
bar-empty-foreground = ${activecolor.foreground-alt}

[module/backlight-acpi]
inherit = module/xbacklight
type = internal/backlight
card = intel_backlight

[module/cpu]
type = internal/cpu
interval = 2
format-prefix = " "
format-prefix-foreground = ${activecolor.foreground-alt}
;format-underline = #f90000
;format-overline = ${self.format-underline}
label = : %percentage:2%%

[module/memory]
type = internal/memory
interval = 2
format-prefix = " "
format-prefix-foreground = ${activecolor.foreground-alt}
;format-underline = #4bffdc
;format-overline = ${self.format-underline}
label = : %percentage_used%%

[module/wlan]
type = internal/network
interface = wlp0s20u3u2
interval = 3.0

format-connected = <ramp-signal> <label-connected>
format-connected-underline = #9f78e1
label-connected = %essid%

format-disconnected =
;format-disconnected = <label-disconnected>
;format-disconnected-underline = ${self.format-connected-underline}
;label-disconnected = %ifname% disconnected
;label-disconnected-foreground = ${activecolor.foreground-alt}

ramp-signal-0 = 
ramp-signal-1 = 
ramp-signal-2 = 
ramp-signal-3 = 
ramp-signal-4 = 
ramp-signal-foreground = ${activecolor.foreground-alt}

[module/eth]
type = internal/network
interface = eno1
interval = 3.0

format-connected-underline = #55aa55
format-connected-prefix = " "
format-connected-prefix-foreground = ${activecolor.foreground-alt}
label-connected = %local_ip%

format-disconnected =
;format-disconnected = <label-disconnected>
;format-disconnected-underline = ${self.format-connected-underline}
;label-disconnected = %ifname% disconnected
;label-disconnected-foreground = ${activecolor.foreground-alt}

[module/date]
type = internal/date
interval = 5

date = "%d-%m"
date-alt = " %Y-%m-%d"

time = %H:%M
time-alt = %H:%M:%S

format-prefix = 
format-prefix-foreground = ${activecolor.foreground-alt}
;format-underline = #0a6cf5
;format-overline = ${self.format-underline}

label = %date% %time%

[module/volume]
type = internal/volume

format-volume = <label-volume> <bar-volume>
label-volume = VOL
label-volume-foreground = ${root.foreground}

format-muted-prefix = " "
format-muted-foreground = ${activecolor.foreground-alt}
label-muted = sound muted

bar-volume-width = 10
bar-volume-foreground-0 = #55aa55
bar-volume-foreground-1 = #55aa55
bar-volume-foreground-2 = #55aa55
bar-volume-foreground-3 = #55aa55
bar-volume-foreground-4 = #55aa55
bar-volume-foreground-5 = #f5a70a
bar-volume-foreground-6 = #ff5555
bar-volume-gradient = false
bar-volume-indicator = |
bar-volume-indicator-font = 2
bar-volume-fill = ─
bar-volume-fill-font = 2
bar-volume-empty = ─
bar-volume-empty-font = 2
bar-volume-empty-foreground = ${activecolor.foreground-alt}

[module/battery]
type = internal/battery
battery = BAT0
adapter = ADP1
full-at = 98

format-full-prefix = " "
format-full-prefix-foreground = ${activecolor.foreground-alt}
format-full-underline = #0f0

format-discharging = <ramp-capacity> <label-discharging>
;format-discharging-underline = ${ramp-capacity-foreground}

ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 
ramp-capacity-4 = 

ramp-capacity-0-foreground = #f00
ramp-capacity-1-foreground = #f70
ramp-capacity-2-foreground = #ff0
ramp-capacity-3-foreground = #7f0
ramp-capacity-4-foreground = #0f0

ramp-capacity-0-underline = ${self.ramp-capacity-0-foreground}
ramp-capacity-1-underline = ${self.ramp-capacity-1-foreground}
ramp-capacity-2-underline = ${self.ramp-capacity-2-foreground}
ramp-capacity-3-underline = ${self.ramp-capacity-3-foreground}
ramp-capacity-4-underline = ${self.ramp-capacity-4-foreground}

format-charging = <animation-charging> <label-charging>
format-charging-foreground = #88f
format-charging-underline = ${self.format-charging-foreground}
animation-charging-foreground = ${self.format-charging-underline}
animation-charging-framerate = 750
animation-charging-0 = ${self.ramp-capacity-0}
animation-charging-1 = ${self.ramp-capacity-1}
animation-charging-2 = ${self.ramp-capacity-2}
animation-charging-3 = ${self.ramp-capacity-3}
animation-charging-4 = ${self.ramp-capacity-4}

[module/temperature]
type = internal/temperature
thermal-zone = 0
warn-temperature = 60

format = <ramp> <label>
;format-underline = #f50a4d
;format-overline = ${self.format-underline}
format-warn = <ramp> <label-warn>
format-warn-underline = ${self.format-underline}

label = %temperature%
label-warn = %temperature%
label-warn-foreground = ${activecolor.secondary}

ramp-0 = 
ramp-1 = 
ramp-2 = 
ramp-foreground = ${activecolor.foreground-alt}

[module/powermenu]
type = custom/menu

expand-right = true

format-spacing = 1

label-open = 
label-open-foreground = ${activecolor.secondary}
label-close =  cancel
label-close-foreground = ${activecolor.secondary}
label-separator = |
label-separator-foreground = ${activecolor.foreground-alt}

menu-0-0 = reboot
menu-0-0-exec = menu-open-1
menu-0-1 = power off
menu-0-1-exec = menu-open-2

menu-1-0 = cancel
menu-1-0-exec = menu-open-0
menu-1-1 = reboot
menu-1-1-exec = sudo reboot

menu-2-0 = power off
menu-2-0-exec = sudo poweroff
menu-2-1 = cancel
menu-2-1-exec = menu-open-0

[settings]
screenchange-reload = true
;compositing-background = xor
;compositing-background = screen
;compositing-foreground = source
;compositing-border = over

[global/wm]
margin-top = 5
margin-bottom = 5

; vim:ft=dosini
